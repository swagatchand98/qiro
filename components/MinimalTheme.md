# Minimal Black & White Theme Transformations Needed

## Completed ✅
- Header: Black with logo
- Settings sidebar: White with underline tabs
- Customer Details section: White card with black accents
- Configuration section: White card with black accents

## Remaining Sections to Update:

### 1. Door Configuration Form
- Update all focus rings from `focus:ring-blue-500` to `focus:ring-black`
- Change "Add Door" button from `bg-blue-600` to `bg-black`
- Update added doors cards from blue gradient to simple white with black border

### 2. Cutting Schemes
- Change section number from `bg-blue-600` to `bg-black`
- Keep amber/brown for visual bars (acceptable accent)
- Update card borders to gray

### 3. Additional Components
- Change section number from `bg-blue-600` to `bg-black`
- Change button from `bg-green-600` to `bg-black`
- Update focus rings to black

### 4. Optional Items
- Change section number from `bg-blue-600` to `bg-black`
- Change button from `bg-purple-600` to `bg-black`
- Update focus rings to black

### 5. Cost Summary
- Change section number from `bg-blue-600` to `bg-black`
- Remove green gradient background, use simple white with black border
- Keep numbers in black

### 6. Export Buttons
- PDF: Change from `bg-red-600` to `bg-black`
- Excel: Change from `bg-green-600` to `bg-white border border-black text-black`
- Text: Change from `bg-gray-600` to `bg-white border border-black text-black`

### 7. Footer
- Change from `bg-gray-800` to `bg-black`
